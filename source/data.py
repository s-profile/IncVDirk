import datetime



class allData:

    curSec = str(datetime.datetime.now().second)
    curMinute = str(datetime.datetime.now().minute)
    curHour = str(datetime.datetime.now().hour)
    curDay = str(datetime.datetime.now().day)
    curMonth = str(datetime.datetime.now().month)
    curYear = str(datetime.datetime.now().year)


